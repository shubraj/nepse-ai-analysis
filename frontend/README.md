# Frontend – React + TypeScript

```bash
npm install
npm run dev
```

Open http://localhost:5173. API requests to `/api` are proxied to the backend (run backend from `../backend`).

Build: `npm run build` (output in `dist/`).
